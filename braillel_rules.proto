syntax = "proto3";

package braillel_rules;

message PartOfSpeech {
  string Name = 1;
  bool BeforeSpace = 2;
  bool AfterSpace = 3;
  bool AllowLineBreak = 4;
}

message Rule {
  PartOfSpeech current = 1;
  repeated PartOfSpeech next = 2;
}

message BrailleRules {
  repeated Rule rules = 1;
}
