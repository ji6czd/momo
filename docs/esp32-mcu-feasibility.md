# ESP32（P4/S3）で momo を動かす ― 実現性メモ

> ステータス（更新 2026-09-05）: **実機で決着**。ESP32-P4 と Milk-V Duo の 2 枚で
> 同一コーパスを実測し、**どちらも実用速度を超えた**（末尾「実機比較」）。
> 以下の本文は着手前（2026-08）の構造からの推定で、**当たった所と外した所がある**。
> 推定の質を確かめる材料として残し、外れた項目には注記を入れた。
> 関連: [`esp32p4-plan.md`](esp32p4-plan.md)（P4 の実装と全実測）、
> [`zerocopy-model-plan.md`](zerocopy-model-plan.md)（借用ロード＝実測して見送り）、
> [`mbm-format.html`](mbm-format.html)（現行レイアウト）。

## 動機

点字ディスプレイのコストはピエゾセルが支配的だが、compute を Android SoC ではなく
数ドルの MCU に落とせれば電子部品の BOM を実際に下げられる。momo（Rust コア）が
ESP32 クラスで動けば、**Android を積まない安価な点字ディスプレイ**が視野に入る。

## チップの見立て（P4 が本命、S3 は苦しい）

| | ESP32-S3 | ESP32-P4 |
|---|---|---|
| コア | Xtensa LX7 ×2 @240MHz | RISC-V ×2 @400MHz |
| 内蔵SRAM | 512KB | ~768KB(L2)（要確認） |
| PSRAM上限 | **~8MB** | **32MB** |
| SIMD | int8(ESP-NN) | AI/SIMD拡張 |
| Rust | Xtensa=espupフォーク | **RISC-V=mainline** |

- 分かれ目は **PSRAM 上限**。w=4 モデルは 11.4MB なので **S3 の 8MB には入らない**。
  P4 の 32MB なら w=4/w=5 とも余裕。
- 「P4 と同じ RISC-V コアの新しい石」があるならそれも RISC-V 系（型番は未確認）。
- **S3/C3 は非対象**という判断でよい。C3 は非力すぎ、S3 は PSRAM が足りない。

## ロジックは載る／モデルが問題

- 推論の可変作業領域は **十数〜数十KB**（スコア配列 `n_classes`1591×f32=6.4KB＋int版6.4KB＋
  特徴量バッファ＋木走査スタック）。コード(.text)も数十〜百KB台。**SRAM に収まる**。
- 常に問題なのはモデル本体（語彙＋重み）だけ。置き場所は **PSRAM** か **flash（XIP）**。

## 速度の見立て ― バーは低い

点字ディスプレイは人間の読速度で「読んでいる行を訳す」用途。必要スループットは
**毎秒せいぜい数十文字**。PSRAM/flash ミス込みで 1〜5ms/文字でも 200〜1000文字/秒出るので
読速度は楽に超える。**指標は per-line のもたつき（数百msストール）を避けられるか**であって、
生スループットではない。バッチ（本一冊）の話とは別。

## 速度レバー（狙う「ホット部分」は小さい）

- 真にランダムなのは **語彙二分探索の“下の方”だけ**。上位10数レベルはどの検索でも同じ
  中央値を叩くのでキャッシュ常駐。→ **上位ピボット索引を SRAM に**（2^11=2048 個＝数十KB）
  or **Eytzinger(BFS順)レイアウト**で bsearch 全体をキャッシュ局所化（2〜4倍既知）。
- スコア配列(6.4KB)はもともとホットで SRAM 向き。CSC 列読み出しは**シーケンシャル**
  （プリフェッチ有効）。→ 実際にキャッシュ敵対的なのは vocab bsearch の末端のみ。
- **計算量ワイルドカードは GBDT 境界（木300本・毎文字・約1MB）**。SRAM には収まらないが
  毎文字ホットなのでキャッシュに上位ノードが乗る。MCU ティアでは**木を浅く/本数を減らした
  軽い境界モデル**にして精度を少し譲る判断がありうる（語彙刈りとは別軸）。

## flash-XIP vs PSRAM

> **注記（2026-09-05・実測で否定）**: 「モデルを借用すれば PSRAM に 11MB を確保せずに済む」
> は P4 実機で**見送りになった**。フラッシュはシリアルで遠く、QIO 化しても
> 語彙 bsearch で 5.46 倍・CSC 読みで 2.13 倍のペナルティが付く（DIO なら 23.3 倍）。
> 詳細は [`esp32p4-plan.md`](esp32p4-plan.md) §7 と [`zerocopy-model-plan.md`](zerocopy-model-plan.md)。

- **S3**: 8MB PSRAM に入らない → モデルを **flash に置いてキャッシュ経由で直接読む**
  （XIP/データmmap、`esp_partition_mmap`→`const u8*`）。PSRAM を空けたまま動く。
  ただし ESP の **flash mmap 窓サイズ上限** に 11MB が収まるか要検証（S3 は苦しい）。
- **P4**: 32MB PSRAM が直接保持。窓もキャッシュも大きく有利。
- どちらも「モデルを借用」できれば PSRAM ヒープに 11MB を確保せずに済む
  → [`zerocopy-model-plan.md`](zerocopy-model-plan.md) と同じものを欲しがる。

## Rust ツールチェーンの現実（「Rust が無い」は誤解）

- **Espressif は Rust を公式サポート**（専任チーム・`esp-rs`・`espup`・`esp-hal`）。
- **RISC-V の ESP（C3/C6/H2/P4）は mainline Rust をそのまま使える**（フォーク不要）。
  → **P4 が本命なのはツールチェーン観点でも正しい**。
- **Xtensa（S3/S2/初代）は espup のフォークが要る**（動くがフォーク）。P4 なら回避。
- 2つのランタイム: **no_std（esp-hal、リーン・将来線）** / **std（esp-idf-hal、std＋スレッド/fs、
  移植が楽）**。

## momors の移植コスト（限定的・書き直しではない）

現状 momors-core は `std`（`#![no_std]` なし）。ただし:

- **ロードは `load_from_bytes(&[u8])` に集約**され、推論経路に fs は焼き込まれていない。
  **momors-wasm が `Predictor::new(&[u8])`→`from_model_bytes` で wasm32 で動いている**ので、
  「fs を切り離しバイト列から読む」形は**すでに実証済み**。
- 数値計算(`Vec`/スライス/`binary_search`/`sort`)と GBDT の木(`Box`)は core/alloc の範囲。
  **外部Cライブラリ依存ゼロ**（LightGBM は木データとして書き出し boundary.rs で純Rust再実装）。

no_std 移植の具体項目:
1. `#![no_std]` ＋ `extern crate alloc`（`Vec`/`String`/`Box`→alloc）。
2. `Read`/`Cursor`/`File` を `&[u8]` スライス読みに置換。**依存の勘所2つ**:
   - `byteorder`: `ReadBytesExt`（`reader.read_u32::<LE>()`、std::io::Read 依存）ではなく
     **`ByteOrder` トレイト（`LittleEndian::read_u32(&buf)`、no_std）** に置換。
   - `thiserror`: 1.x は std 専用。**no_std 対応版(2.x)か手書きの Error enum** に置換。
3. `std::path`/`std::io::Error` のエラー文脈をスタブ化。
4. グローバルアロケータ（esp-hal/esp-idf の PSRAM アロケータ）。ただし flash 借用に倒せば
   巨大 PSRAM 確保自体を回避できる。

## 進め方（二段構え）

1. **プロトタイプ: `std` on ESP-IDF（P4）**。momors-core をほぼそのままコンパイルし、
   flash パーティションを mmap→`&[u8]`→`from_model_bytes`、**1文字の推論時間を実測**。
   「載る/速い」を no_std 投資の前に確定。
2. **本番: no_std（esp-hal）** で footprint を絞る。
3. materialize コストが問題なら **ゼロコピー＋flash借用**へ（別ブランチの本丸）。

**最初のマイルストーン**: *w=4 を flash に焼いて mmap し、P4 で1文字の推論時間を測る*。
これで載る/載らない・速い/遅いが一発で決まる。

## 未確定・要実機

> **注記（2026-09-05）**: 下 4 点のうち 2 点は決着した。残りは 2 点。

- ~~flash mmap 窓のサイズ上限~~ → **決着**: P4 で 12MB パーティション全域が一括で
  `esp_partition_mmap` できた（S3 は未検証のまま）。
- PSRAM アロケータ（esp-hal 側）の実運用成熟度。← no_std は未着手なので未確定のまま。
- ~~per-文字レイテンシの実数~~ → **決着**: P4 551µs/字・Duo 343µs/字（下の「実機比較」）。
- 「P4 と同じコアの新系列」の正確な型番と入手性（国内流通は未確認）。
- **新規**: w4 と w5 の**精度差**。w5 を動かすためにハードを選び直す価値があるかは、
  この数字が無いと決められない（PC 上の回帰セット/recall_audit で測れる）。

## メモ

電子工作は誤って壊すと悲しいので、モジュールは予備込みで数枚買っておく方針。

---

## 実機比較（2026-09-05）― ESP32-P4 と Milk-V Duo

同じ w4 モデル・同じコーパス（`dataset/basic_raw.tsv` の原文を約 400 字＝原稿用紙 1 枚ずつに
束ねた 135 行・**55,675 字**）を、かな変換＋点字変換まで通して両機で実測した。

| | **ESP32-P4** | **Milk-V Duo** |
|---|---|---|
| SoC | ESP32-P4 v1.3, RISC-V 360MHz ×2 | SOPHGO CV1800B, T-Head C906 1GHz |
| メモリ | PSRAM 32MB（HEX 16bit, 200MHz） | パッケージ内 **DDR2 64MB**（実効 55MB） |
| モデル置き場 | フラッシュ 16MB（QIO, `esp_partition_mmap`） | SD カード上の rootfs |
| OS | ESP-IDF（ベアメタル + FreeRTOS） | Linux 5.10 |
| 55,675 字 | 30.66 秒 | **19.1 秒** |
| 1 文字 | 551 µs | **343 µs** |
| スループット | 1,816 字/秒 | **2,912 字/秒** |
| **原稿用紙100枚（＝点字本 1 冊）** | 22.0 秒 | **13.7 秒** |
| モデル展開 | **1.00 秒** | 1.45 秒 |
| 電源投入 → 変換可能 | **1.2 秒** | Linux 起動 + 1.45 秒 |
| 常駐 | 14.5MB | 約 17MB |
| w5（24.9MB） | ✗ **フラッシュ 16MB に焼けない** | ✗ **推論中に OOM Kill** |
| 価格帯 | モジュール $5〜8 | ボード $5〜9（国内 2千円未満） |

### 分かったこと

1. **どちらも実用線を超えた**。「1 秒 1,000 字を割ると実用上つらい」に対し、P4 で 1.8 倍、
   Duo で 2.9 倍の余裕。**ハード選定の軸は速度ではなくなった**。
2. **Duo が 1.6 倍速いのはコアの差ではない**。C906 は 1GHz 単発イン
   オーダで、P4 の 2 コア 360MHz + L2 512KB より豪華ではない。効いているのは
   **本物の DDR かどうか**。この処理は 10.2MB の語彙をランダムに叩き続けるので、
   コアの強さよりメモリの近さで決まる。
3. **w5 はどちらでも動かない。ただし理由が違う**。P4 はフラッシュ 16MB に 24.9MB を
   焼けない（RAM ではない）。Duo は載るがメモリが足りず推論中に OOM Kill
   （死ぬ前で既に 1,457 µs/字 = w4 の 4.2 倍）。**w5 を動かすなら、フラッシュ 32MB の
   P4 モジュールか、RAM の多いボード（Duo 256M / SG2002, $9〜13）**。
   ただしその前に **w4 と w5 の精度差**を測るのが順序（未実施）。
4. **借用ロード（mmap）は Duo でも w5 を救わない**。二分探索の経路は文章を通すと配列の
   ほぼ全域に散らばるため（PC 実測: ヒット 49% でも常駐は 14% しか減らない）、
   mmap しても 20MB 前後はページキャッシュに居座りたがる。**rootfs が SD カード**なので
   フォールトは 0.3〜1ms 級で、「OOM Kill がスラッシングに変わるだけ」。効かせるには先に
   [`zerocopy-model-plan.md`](zerocopy-model-plan.md) の**特徴量タイプ別サブテーブル化**（未検証）が要る。

### どちらを選ぶか ― 速度ではなく、どのスタックで生きたいか

| | P4 が有利 | Duo が有利 |
|---|---|---|
| 起動 | **1.2 秒**（Linux を待たない） | |
| 堅牢性 | OOM Killer がいない・SD カード不要（可動部/FS 破損なし）・遅延が決定的 | |
| 周辺 | セル駆動(SPI/HV シフトレジスタ)・6 キー(GPIO)・音声(I2S)・USB OTG・SD+FAT が**全部 1 チップに載る**（ESP-IDF に FATFS/SDMMC 同梱） | |
| 統合 | | **Linux**。USB ガジェット、既存の点字スタックに入って liblouis を置き換える道 |
| 速度・RAM | | 1.6 倍・55MB |

**単なる点字ディスプレイ（PC の周辺機器）なら、USB HID とセル駆動だけなのでもっと小さい
MCU で足りる。P4 が必要になるのは、その機械が自分で点訳するとき**——つまり momo を
デバイス内で走らせるという前提が、そのまま P4 を選ぶ理由になっている。
逆に Linux（Duo 系）を選ぶ動機は速度ではなく、[project-liblouis-integration] の
統合パスのほうにある。

### 当時の推定の答え合わせ

| 上の本文の推定 | 実際 |
|---|---|
| 「必要スループットは毎秒せいぜい数十文字、1〜5ms/文字でも足りる」 | ○ 方向は正しい。実測は 0.34〜0.55ms/文字で**推定より 1 桁速かった** |
| 「真にランダムなのは語彙二分探索の下の方だけ」 | ○ `:probe` 実測で lookup 73µs/字（総計の 13%）。ただし**読みモデル全体（lookup + CSC 読み）が最大項** |
| 「計算量ワイルドカードは GBDT 境界（毎文字 300 本）」 | △ 当時は正しかったが、反転索引化で境界は 100µs/字（21%）まで落ち、**重心は読みモデルへ移った** |
| 「flash-XIP でモデルを借用すれば PSRAM を空けられる」 | ✗ **実測して見送り**。フラッシュがシリアルで遠く、語彙 5.46 倍・CSC 2.13 倍のペナルティ |
| 「P4 は RISC-V なので mainline Rust が使える」 | ○ そのとおり。Milk-V Duo も `riscv64gc-unknown-linux-musl` の静的バイナリで動いた |
| 「モジュールは予備込みで数枚」 | ○ 実際に v1.3 チップ固有の設定でブートループを何度も踏んだ |
