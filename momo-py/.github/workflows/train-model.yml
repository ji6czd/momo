name: Train Model

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      model_name:
        description: 'モデル名（オプション）'
        required: false
        default: 'model'

jobs:
  train:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
      
      - name: Train model
        run: |
          momo train --tsv=dataset/training_data.tsv
      
      - name: Upload model artifact
        uses: actions/upload-artifact@v4
        with:
          name: trained-model
          path: ./dataset/training_data.zip
          retention-days: 7